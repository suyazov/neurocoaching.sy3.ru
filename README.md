# Digital Belka — Neurocoaching

Control plane for `neurocoaching.sy3.ru`.

This private repository intentionally contains onboarding documentation only. It has no application code, executable product TASK, deployment configuration, DNS configuration, or production actions.

Current capability: `docs-only`; delivery disabled; production disabled.
Requested future capability intent: `wordpress-staging`.

The first executable product TASK may be created only after a separate capability activation and `PROJECT_ORCHESTRATION_READY`.
